apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/emcgowan/jm9:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/emcgowan/jm9:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: jm9
        chartPath: charts/jm9
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/emcgowan/jm9'
          image.tag: '{{.DIGEST_HEX}}'
